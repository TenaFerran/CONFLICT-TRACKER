spring:
  application:
    name: conflict_tracker
  profiles:
    active: prod
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      encoding: UTF-8